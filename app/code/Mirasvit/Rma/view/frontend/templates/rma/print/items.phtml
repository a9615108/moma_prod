<?php
/* @var $block \Mirasvit\Rma\Block\Rma\View\Items */
?>
<div class="order-details-items ordered">
    <div class="table-wrapper order-items">
        <table class="data table table-order-items" id="my-orders-table" summary="Items Ordered">
            <caption class="table-caption"><?= __('RMA Items') ?></caption>
            <thead>
                <tr>
                    <th class="col name"><?= __('Product Name') ?></th>
                    <th class="col sku"><?= __('SKU') ?></th>
                    <th class="col qty"><?= __('Qty') ?></th>
                    <th class="col qty"><?= __('Weight') ?></th>
                </tr>
            </thead>
            <tfoot>
                <?= $block->getChildHtml('order_totals') ?>
            </tfoot>
            <tbody>
            <?php foreach ($block->getItems() as $item): ?>
                <tr class="border">
                    <td class="col name" data-th="Product Name"><strong class="product name product-item-name"><?= $block->getOrderItemLabel($item) ?></strong></td>
                    <td class="col sku" data-th="SKU"><?= $block->getOrderItemSku($item) ?></td>
                    <td class="col qty" data-th="Qty"><?= $item->getQtyRequested() ?></td>
                    <td class="col qty" data-th="Weight"><?= $block->getItemWeight($item) ?></td>
                </tr>
            <?php endforeach?>
            </tbody>
        </table>
    </div>
</div>